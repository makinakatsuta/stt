# 全盲向けアクセシビリティ再チェック

確認日：2026-09-24。対象：v3.31.27への更新前のワークスペース（2026-09-24）。

## 評価範囲と結論

ユーザー報告により、Windows Firefox・Google Chrome・Microsoft Edge、Android Google Chromeでプレイ可能なことは検証済み。ロービジョン向け配色・拡大表示は今回の対象外であり、評価を下げる理由にしない。再チェックで見つかった3件は同日修正し、回帰テストを通過した。ブラウザーごとの実機読み上げを今回エージェントが再試験したものではない。

## 確認できたこと

- 読み上げ方式の選択・保存、保存拒否時の継続、SpeechSynthesis APIがない場合のライブ領域への通知。
- サーブ・返球ボタンの支援技術によるclick操作、タップの二重入力防止、ボタンの標準Space操作。
- キーボード操作、中断メニューのTab循環、ヘルプの見出しへのフォーカス移動、再開・結果画面の操作先へのフォーカス移動。
- 設定保存と復元、難易度選択、中断・ヘルプ・再開、ゲーム交代。

既存の docs/accessibility.test.cjs と docs/settings-flow.test.cjs をNodeのVMで実行し成功。DOM・音声はモックであり、実機の発声品質を測定するテストではない。追加のモック実行とコード確認で以下を確認した。

## 検出した問題（以下は修正前の記録）

1. **優先度：高。得点後の通知が読み上げ方式を迂回する。** docs/js/game-engine.js:1573–1605。ミス方向とスキップ案内がsr-announcerを直接更新するため、内蔵音声を選んでもスクリーンリーダーへ通知される。スクリーンリーダー方式では、得点コールの50ms後の更新を方向通知が80ms後に上書きする。実際の発声の遮断・重複の程度はリーダー依存だが、通知先の一本化が不完全なことはコードで確認できる。内蔵音声方式でも遅延スキップ通知がライブ領域に入ることを追加実行で再現した。通知を共通の音声管理へ集約し、得点コールを失わない順序にする必要がある。

2. **優先度：高。中断中にCPUのサーブ準備が進む。** docs/js/game-engine.js:1256–1262。CPUの遅延処理は状態だけを確認し、中断を確認しない。中断したままPRE_SERVE_READYからPRE_SERVE_HEARDへ進み、「いきます」が発声されることを追加実行で再現した。再開時に返事が必要な状況を取り違えるおそれがある。中断中は遅延処理も保留し、再開時に残り時間と案内を整合させる必要がある。

3. **優先度：中。内蔵音声の実行エラーで代替通知がない。** docs/js/speech-system.js:72–75。API自体がない場合の代替通知はあるが、speakが例外を投げる場合はコンソールへの警告だけで、ライブ領域は空のまま。例外を注入して再現した。非同期の発声エラーにもハンドラーがない。発声失敗時に必要な案内を代替通知する必要がある。

## 修正結果

- 得点後のミス方向・スキップ案内による直接ライブ通知を除去し、判定・得点者・スコアの審判コールに統一した。スキップ方法は既存のヘルプとボタン説明で確認できる。CPUの打球通知も共通の打球音に統一し、ライブ領域への直接更新を除去した。
- ゲーム用の遅延処理を管理し、中断時は残り時間を保存して停止、再開時は残り時間から続行する。CPUの準備・返事・サーブ、遅延得点コール、得点後の待ち時間、次ゲームへの移行を対象とした。終了・新試合・スキップ時には不要になった処理を取り消す。
- 内蔵音声の同期例外・非同期発声エラーをスクリーンリーダー用通知で補う。停止・方式変更・次の案内が入った後の古いエラーや保留通知は破棄する。
- 変更したJavaScriptの読み込みURLを更新し、ブラウザーに古いコードが残ることを防ぐ。修正を単体配布対応と合わせてv3.31.27に収録した。

## 修正後の検証

docs/accessibility.test.cjs に上記の回帰テストを追加。得点通知の方式別動作、中断中の無発声・状態維持、残り時間での再開、終了・新試合・スキップ・ゲーム交代での取消、同期・非同期の音声失敗と古い通知の破棄が成功した。

accessibility、settings-flow、easy-rally、normal-rally、hard-rally の全5テストが成功。Node VM内で実行し、normal-rallyはプロセス終了の代わりにPromiseの完了を待機して、実際のmain.wasmを含む統合テストを完了した。画面DOMと音声のテストにはモックを使用する。

ゲームの物理処理・Goコードは変更していない。今回の修正後の実機読み上げは未実施。ユーザーによる従来のプレイ検証済み環境の記録は維持する。


公開準備時に通常のNode実行でも全5テストの成功を確認した。WASMを公開済みソースから再ビルドし、JS/WASM統合テストも成功。単体配布用のGoサーバーに埋め込み資産のHTTP配信テストを追加して成功した。
