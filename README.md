# サウンドテーブルテニス (STT) オンライン

サウンドテーブルテニス (STT) は、視覚障害者のために考案された卓球競技です。
このプロジェクトは、STTの公式ルールに準拠し、**視覚障害者（モバイル/PCのスクリーンリーダー利用者）も健常者も共に同じ条件で対戦できる**ことをコンセプトとした、GoバックエンドおよびWeb Audio API搭載のリアルタイムブラウザゲームです。

---

## 🌟 主な特徴

1. **Web Audio API による3D立体音響システム**
   * ボールの左右の移動をステレオパンニングで、奥行き（相手コート、ネット、自分コート）を音量と音質（ローパスフィルターによるこもり具合）の変化でシミュレートします。
   * 金属球が4個入ったSTT公式ボール特有の「シャラシャラ」という転がり音をオシレーターとノイズを用いてリアルタイムに合成します。
   * 木製ラケットで打球した時の「コン」、フレームに当たった時の「カツ」、ネットに当たった時の「ポス」など、素材感のある衝突音を再現しています。
   * **チャージサーブ音響**: スペースキー長押し中にピッチが上昇するチャージ音を合成。長押し時間（最大1.5秒）に応じてサーブ速度が変化します。
   * **ミス方向ガイド音**: 打ち返しミス時に「左/右/中央を通りました」と方向付きのステレオ音響でフィードバック。スクリーンリーダー利用者も次のラリーに活かせます。
   * **ボール停止位置音**: ボールが停止した際に、停止X座標にパンニングした「コトン」音を再生。「どこで止まったか」が音で把握できます。
   * **音量ダイナミクスの対数スケール化**: ボール転がり音の音量を対数スケールで計算し、低速域の微妙な音量変化をより豊かに表現。

2. **完全アクセシビリティ（PC・スマホ両対応、スクリーンリーダー＆音声ガイド）**
   * Web Speech API (`SpeechSynthesis`) による自動音声案内（実況）を搭載。審判のコールや、サーブ前の掛け合い（「いきます」「はい」）をすべて音声で実況します。
   * スクリーンリーダー（iOSのVoiceOver、AndroidのTalkBack）の操作を妨げないセマンティックなHTML構造とARIA属性設計。
   * パソコンではキーボードの最小限のキー（左右矢印とスペースキー）、スマートフォンでは「端末の傾き（チルト）」と「画面タップ」のみで直感的に対戦プレイまで完結します。
   * **音声速度設定**: ウェルカム画面のスライダーで読み上げ速度を0.5〜2.0倍に調整可能。設定はlocalStorageに保存され次回起動時に復元されます。スクリーンリーダー熟練者は速い読み上げに設定できます。

3. **Go ＋ WebSocket によるリアルタイムオンライン・クロスプラットフォーム対戦**
   * 1台のPCでのCPU対戦に加え、GoバックエンドによるWebSocket通信を利用したオンライン対戦に対応。
   * パソコン同士だけでなく、**「パソコン vs スマートフォン」**、**「iPhone vs Android」**といったデバイスの壁を越えたリアルタイム対戦（クロスプレイ）が可能です。
   * ゲームステート（サーブ制限時間、デュース判定、得点処理）はGo側で厳密に判定・同期されるため、ラグがあっても一貫したゲーム進行が可能です。
   * **リマッチ機能**: 試合終了後に「もう一度プレイ」ボタンで、同じ相手・同じWebSocket接続のままリマッチが可能です。
   * **切断時の5秒猶予**: 対戦相手が切断した際、5秒間の再接続待機を設けます。同じルームIDで再接続できた場合は試合を継続します。
   * **観戦モード**: 試合中のルームにルームIDを入力して接続すると、`role: 3`（観戦者）として参加できます。最大5名まで観戦可能で、観戦者はゲーム音響・テキストをリアルタイムで受け取れます。

4. **ゲームプレイ体験の向上**
   * **CPU戦のサーブ交代**: Normal/Hard難易度では2ポイントごとにサーブ権が交代。プレイヤーがサーブする場面でSTT本来の心理戦（「いきます/はい」）を体験できます。Easy難易度はCPUサーブ固定のまま（ラリー練習重視）。
   * **チャージサーブ**: `STATE_SERVE_WAITING` 中にスペースキーを長押しすると、長押し時間（最大1.5秒）に応じてサーブ速度（vy: 4.5〜8.0）が変化します。
   * **打ち返し判定の難易度別調整**: 判定幅をEasy=50px、Normal=30px、Hard=20pxに設定し、難易度に合った打ち返しやすさを実現。
   * **インターバルスキップ**: 得点後4秒の待機中に、スペースキーまたはタップで即座に次のラリーへ進めます。
   * **試合結果画面**: 試合終了後に勝者・最終スコアを表示し、「もう一度プレイ」「メニューに戻る」の選択ができます（従来の6秒自動退出を廃止）。
   * **設定の永続化**: 難易度、チルトON/OFF、サーバーアドレス、音声速度をlocalStorageに保存し、次回起動時に自動復元します。

5. **プレミアムな視覚デザイン（健常者向け）**
   * ダークモードを基調としたネオン・サイバーパンク調の美しいグラフィック。スマートフォンの表示にも完全対応したレスポンシブ設計。
   * ボールの軌跡や、衝突時にコート上に広がる「音の波紋（パルス）」を可視化。音の動きを視覚的にも体感できます。
   * **STT公式規格・寸法ガイドの可視化**: 卓球台の正確な寸法（長さ274cm、幅152.5cm、サイドフレーム60cm、ネット高さ15.25cm、ネット下の隙間4.2cm）を、ゲーム画面内のネオン調寸法線と、右側のリアルタイム寸法規格サイドパネル（SVG図解付き）で視覚的に表現しました。見える人にとってもSTTの独特なルールスケールが一目で理解できます。

---

## 🎮 遊び方・操作方法

使用するデバイスやアクセシビリティ設定に合わせて、最適な操作方法を選択できます。

### 1. パソコンでの操作（標準キーボード）
* **左矢印キー (`ArrowLeft`)**: ラケットを左に移動
* **右矢印キー (`ArrowRight`)**: ラケットを右に移動
* **スペースキー (`Space`)**: アクション（サーブの準備、相手への返答、メニュー決定、ラリー中の打ち返し）
* **エスケープキー (`Escape`)**: ゲームの一時停止 / メニューに戻る

### 2. スマートフォンでの操作（チルト ＋ 画面タップ）
スマホ単体でプレイする場合、タッチドラッグ操作がスクリーンリーダーにブロックされるのを防ぐため、以下の操作になります。
* **ラケットの移動（チルト操作 ＋ 足音ガイド）**:
  * スマートフォン本体を**左右に傾ける**ことで、ラケットが左右に移動します。
  * 傾き角度に応じた**比例制御**を採用しています。±4度以内のデッドゾーン（不感帯）があり、**左右±30度**でラケットが最大速度に達します。傾ける角度が大きいほど速く、小さいほどゆっくり移動します。
  * **ラケット移動時のステレオ音響**: ラケットを動かすと、その位置に応じたステレオパンニング（左右の定位）と微妙な音色変化（中央に近づくほどクリアになる）の足音が鳴ります。これにより、スピーカーのちょうど真ん中（パン値0）でボールを待ち構えやすくなりました。
  * プレイ開始時に、その時の手の持ち角度に合わせて**自動的にキャリブレーション（水平補正）**が行われます。
  * プレイ中、持ち方を変えたくなったら画面下部の**「チルトをリセット」**ボタンをタップ（またはダブルタップ）することで、いつでも中心位置を補正できます。
* **アクション（決定/サーブ/打ち返し）**:
  * **画面全体のどこでもタップ**（VoiceOverやTalkBackなどの**スクリーンリーダー起動時は画面の任意の場所をダブルタップ**）することで、スペースキーと同じアクションが発生します。
* **ボール接近ビープガイド**:
  * ラリー中にボールが自分の守備ラインに近づくと、**3段階の「ぴ」ビープ音**でタイミングを案円します（ヘッドフォン準備を推奨）。
    * 🔊 低い「ぴ」→ ボールが近づいています
    * 🔊🔊 高い「ぴ」→ 打ち返しゾーンに接近
    * 🔊🔊🔊 高く鋭い「ぴっ」→ 今すぐタップ/スペースキーで打ち返せます
* **設定の切り替え**:
  * 開始時のウェルカム画面で「スマートフォンの傾き（チルト）で操作する」のチェックを外すと、スマホに接続した外付けのBluetooth物理キーボード（パソコンと同じ操作）で遊ぶことも可能です。

---

### STT公式ルールに基づくプレイフロー
1. **主審の「プレー」宣告**でラリーの準備が始まります。
2. **サーバー**: スペースキーまたは画面タップ（ダブルタップ）を押してボールを静止させ、準備ができたら「いきます」と発声します。（10秒以内）
3. **レシーバー**: サーバーの「いきます」に対してキー/タップで「はい」と返答します。（5秒以内）
4. **サーバー**: 返答から5秒以内にキー/タップでサーブを打ちます。（5秒以内）
5. **ラリー開始**: ボールがネットの下（テーブル面上4.2cmの隙間）を通って転がってくるので、音（左右の定位や音量）を頼りにラケットを動かしてタイミングよく打ち返します。
6. **得点判定**: ボールが相手の守備コート内で「停止」するか、エンドフレーム（奥の壁）の内側に当たって「セーフ」になれば得点です。フレームを越えて飛び出たり（アウト）、ネットに引っかかったり、ラケット以外の場所に当たると相手の得点になります。

---

## 🛠️ 技術スタック

* **バックエンド**: Go (Gorilla WebSocket)
* **フロントエンド**: HTML5, Vanilla CSS (CSS Variables, Flexbox, Grid), Vanilla JavaScript (Canvas API, Web Audio API, Web Speech API, DeviceOrientation API)

---

## 🚀 起動方法

### 方法1：実行ファイルから起動（推奨・最も簡単）
1. プロジェクトルートにある `stt.exe` をダブルクリックして実行します。
2. サーバーが起動し、自動的にデフォルトのブラウザで `http://localhost:8080` が開きます。

### 方法2：Goソースコードから起動（開発者向け）
1. ターミナルでプロジェクトディレクトリに移動します。
   ```bash
   cd stt
   ```
2. 以下のコマンドでサーバーを起動します。
   ```bash
   go run main_server.go
   ```
3. サーバーが起動すると、自動的にブラウザで `http://localhost:8080` が開きます。

---

## 🌐 別のパソコン・スマホと対戦する場合の手順（ローカルネットワーク対戦）

同じWi-Fi（ローカルネットワーク）に接続された別のPCやタブレット・スマートフォンと対戦（クロスプレイ）することができます。

### 1. サーバーPC（親機）の準備
1. 親機となるPCで `stt.exe` を起動します。
2. 親機PCのローカルIPアドレス（IPv4）を調べます。
   * **Windowsの場合**: コマンドプロンプトを開き、`ipconfig` と入力して実行。「IPv4 アドレス」の値を確認します（例: `192.168.1.15`）。
   * ※セキュリティ警告が出た場合は、プライベートネットワークでの通信を許可してください。

### 2. 対戦相手（子機PC または スマートフォン）の接続
1. 対戦相手のPCやスマートフォンを、親機と同じWi-Fiネットワークに接続します。
2. 子機のWebブラウザを開き、アドレスバーに **「親機PCのIPアドレス:8080」** を入力してアクセスします。
   * 例: `http://192.168.1.15:8080`
   * ※スマートフォンのチルトセンサーを使用する場合、ブラウザによっては安全な接続（HTTPS）またはローカルネットワーク通信の許可が必要です。
3. 画面が表示されたら、「オンライン対戦」を選択します。

### 3. ルームに入って対戦開始
1. 親機・子機の双方で、ルームIDを**空欄のまま**「ルームに入る」ボタンを押します。（特定の相手とだけ遊びたい場合は、両方に同じルームIDを入力してボタンを押してください。）
2. これで自動的にマッチングし、対戦が始まります。

---

## 🔧 更新・バグ修正履歴

### v3.0.0 — 2026-06-13: ゲームプレイ体験・音響・オンライン機能の大規模アップデート

#### 🎮 ゲームプレイ体験

| # | 対象ファイル | 変更内容 |
|---|---|---|
| 1 | `docs/app.js` | **CPU戦のサーブ交代制**: Normal/Hard難易度で2ポイントごとにサーブ権が交代するよう変更。プレイヤー側からサーブを打つ場面でSTT本来の「いきます/はい」の掛け合い体験が可能に。Easy難易度はCPUサーブ固定を維持 |
| 2 | `docs/app.js` | **チャージサーブ**: `STATE_SERVE_WAITING` 中にスペースキーを長押しすると長押し時間（最大1.5秒）に応じてサーブ速度が変化（vy: 4.5〜8.0）。長押し中はピッチが上昇するチャージ音を再生 |
| 3 | `docs/app.js` | **打ち返し判定の難易度別調整**: 判定幅をEasy=50px / Normal=30px / Hard=20pxに変更。ミス時に「左/中央/右を通りました」をsr-announcer経由でアナウンス |
| 4 | `docs/app.js` | **インターバルスキップ**: 得点後の4秒待機中に、スペースキーまたはタップで即座に次のラリーに進める機能を追加 |
| 11 | `docs/app.js` | **試合結果画面の改善**: 試合終了後に勝者名・最終ゲームスコアを表示し「もう一度プレイ」「メニューに戻る」ボタンを表示。従来の6秒自動退出を廃止 |

#### 🔊 音響・アクセシビリティ

| # | 対象ファイル | 変更内容 |
|---|---|---|
| 5 | `docs/app.js` | **ミス方向ガイド音**: `playMissSound()` に `x` パラメータを追加しステレオパンニングを適用。ボールが通過した横位置（左/中央/右）の方向から失点音が鳴り、次のラリーに活かせるフィードバックを提供 |
| 6 | `docs/app.js` | **ボール停止位置音**: 新メソッド `playBallStopSound(x)` を追加。ボール停止時に停止X座標にパンニングした「コトン」音（三角波、280→120Hz）を再生し、停止位置を音で把握できるよう改善 |
| 7 | `docs/app.js` | **音量ダイナミクスの対数スケール化**: `updateBallSound()` 内の音量計算を対数スケール（`Math.log1p`）に変更。低速域での微妙な音量変化が豊かになり、転がりの減速感がよりリアルに |
| 8 | `docs/app.js` `docs/index.html` `docs/styles.css` | **音声速度設定**: ウェルカム画面に速度スライダー（0.5〜2.0倍）を追加。`SpeechSystem` に `speechRate` プロパティを追加しlocalStorageと連携。スクリーンリーダー熟練者向けに高速読み上げをサポート |

#### 🌐 オンライン対戦

| # | 対象ファイル | 変更内容 |
|---|---|---|
| 13 | `docs/app.js` | **リマッチ機能**: 試合終了後「もう一度プレイ」ボタンで同一WebSocket接続のままリマッチ可能に。`rematch_offer` / `rematch_accept` アクションを追加し両プレイヤーが同期して再試合を開始 |
| 14 | `main_server.go` | **切断時の5秒再接続猶予**: プレイヤー切断時に即終了せず `opponent_disconnected` を通知し5秒間待機。同じルームID・クライアントIDで再接続した場合は試合を継続。`disconnectedPlayers` マップと `reconnectTimers` を追加 |
| 15 | `main_server.go` | **観戦モード（Observer）**: 試合中のルームへ `role: 3` で最大5名まで観戦参加が可能に。観戦者はすべてのブロードキャストを受信するが、アクション送信は無視される。`observers` マップを追加 |

#### 💾 UX・設定管理

| # | 対象ファイル | 変更内容 |
|---|---|---|
| 17 | `docs/app.js` | **設定の永続化**: 難易度・チルトON/OFF・サーバーアドレス・音声速度をlocalStorageに保存し、次回起動時に自動復元 |

### v2.6.3 — 2026-06-07: プレイヤー打球時の正解音（チャイム）追加

| # | 対象ファイル | 変更内容 |
|---|---|---|
| 1 | `docs/app.js` | プレイヤーがボールを打ち返した際に「ティン！」という正解音（チャイム）を追加。特に初級編（Easy）では音程・音量・長さが強調され、初心者が成功体験をより直感的に得られるよう改善 |

### v2.6.2 — 2026-06-06: 試合終了時の歓声音（拍手）の追加

| # | 対象ファイル | 変更内容 |
|---|---|---|
| 1 | `docs/app.js` | 試合終了時に観客の歓声・拍手（`playCheerSound`）を再生するよう追加。LFOとバンドパスフィルターを使って生成された音で達成感を向上 |

### v2.6.1 — 2026-06-06: ラケット移動音のステレオ定位強化（中央待機支援）

| # | 対象ファイル | 変更内容 |
|---|---|---|
| 1 | `docs/app.js` | `playFootstepSound` のラケット移動音において、位置(X座標)に基づく**ステレオパンニング**に加え、**音色(ピッチ・Q値)の微妙な変化**を付加。中央(pan=0)に近づくほど音がクリアになり、スピーカーの中心でボールを待ち構えるのが直感的に行えるよう改善 |

### v2.6.0 — 2026-06-06: プレイ集中モード実装 ＆ ボール接近ビープ音ガイドの追加

| # | 対象ファイル | 変更内容 |
|---|---|---|
| 1 | `docs/app.js` | **プレイ集中モード**: プレイ中は `play-instructions` テキストフィールドを自動非表示にし、右紓の勁を剪ってゲーム画面全体での操作に集中できるよう改善 |
| 2 | `docs/app.js` | **プレイ画面全体タップ対応**: `canvas-container`内のみだったタップ検知対象を `screen-play` 全体に拡張。画面のどこをタップ/ダブルタップしてもアクションを実行可能に |
| 3 | `docs/app.js` | **ボール接近ビープ音ガイド**: `playBeep()` メソッドを新設。ラリー中、ボールが守備ラインに近づくと far（120px圏内、880Hz）→ near（60px圏内、1320Hz）→ hit（30px圏内、1760Hz）の3段階で「ぴ」ビープ音を鳴らす |
| 4 | `docs/app.js` | **`updateApproachBeep()`メソッド追加**: 毎フレーム呼び出され、ボールの向き・Y座標で接近段階を判定。200msの最低間隔で過剰発火を防止。hitゾーン初回到達時はaria-liveで「今です！」をアナウンス |

### v2.5.0 — 2026-06-06: スマホチルト比例制御の改善 ＆ 簡単モードのラリー練習特化

| # | 対象ファイル | 変更内容 |
|---|---|---|
| 1 | `docs/app.js` | スマホのチルト操作を**±30度比例制御**に改良。±4度のデッドゾーンを設け、±30度でラケット最大速度に達するリニアスケールを実装。傾き角度に応じた細かい速度調整が可能に |
| 2 | `docs/app.js` | **簡単モード**を「打ち負かす」から**「ラリー練習重視」**に変更。CPUサーブを低速・ほぼ直進（vx=±0.4）に設定し、CPUは確実に打ち返す追従速度に調整。STT初心者が長いラリーを体験できるよう設計 |

### v1.1.0 — 2026-06-03: オンライン対戦の不具合修正

オンライン対戦機能が正常に動作しない原因となっていた4件のバグを修正しました。

| # | 対象ファイル | 問題の要因 | 修正内容 |
|---|---|---|---|
| 1 | `main_server.go` | `ping` メッセージが相手にブロードキャストされる | broadcastループ内で `ping` をインターセプトし送信者に `pong` を返す処理を追加 |
| 2 | `docs/app.js` | パドル位置を毎フレーム（約60回/秒）送信しサーバーを順起き | `syncPaddlePosition` に30msスロットルを実装 |
| 3 | `docs/app.js` | 両プレイヤーがローカルでボール物理を計算し、ネット遅延でボール位置がズレる | Player2はボール物理計算をスキップ、`serve`/`ball_hit` 受信時のみ位置を更新 |
| 4 | `docs/app.js` | Player2が `score` イベントをローカルで処理し副作用が発生 | `score` イベント先頭でPlayer2は即リターン、ホスト(Player1)の `point` メッセージのみで得点を更新 |

---

## 📄 ライセンス

このプロジェクトは [MIT ライセンス](file:///Z:/stt/LICENSE) に基づいてライセンスされています。
